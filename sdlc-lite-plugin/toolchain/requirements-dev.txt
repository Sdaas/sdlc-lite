# Pinned dev toolchain for /implement-feature.
# This is the SINGLE SOURCE OF TRUTH for the tools every gate relies on. It is
# installed into the dev container (see .devcontainer postCreateCommand) so the
# Gate 0 preflight finds them. The workflow assumes it runs inside that container.
#
# NOTE: these are conservative floors. After the first clean install inside the
# container, FREEZE exact versions (`uv pip freeze`) into a lockfile so runs are
# reproducible. Do not rely on "latest".

ruff>=0.6            # lint + format         (IMPLEMENT inner loop)
mypy>=1.11           # static type checking  (IMPLEMENT inner loop) — CI-gate checker
pytest>=8.0          # unit/api/e2e tests    (IMPLEMENT inner loop)
pytest-cov>=5.0      # coverage              (CODE-REVIEW gate)
mutmut>=2.5          # mutation testing      (CODE-REVIEW gate)
hypothesis>=6.100    # property-based / stress (concurrency + edge cases, when applicable)
pytest-asyncio>=0.23 # async test support    (concurrency features, when applicable)
